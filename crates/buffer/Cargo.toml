[package]
name = "saguarodb-buffer"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
common = { package = "saguarodb-common", path = "../common" }
parking_lot.workspace = true
